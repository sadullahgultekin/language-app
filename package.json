{
  "name": "language-app",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "sed -i.bak \"s/vocab-__BUILD_TS__/vocab-$(date +%s)/g\" public/sw.js && wrangler deploy; mv public/sw.js.bak public/sw.js",
    "db:migrate:local": "wrangler d1 execute vocab-db --local --file=migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute vocab-db --remote --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "hono": "^4.7.0"
  }
}
